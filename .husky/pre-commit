#!/bin/sh
# Run typecheck so broken code (e.g. missing imports, type errors) doesn't get committed.
# Full `bun run check` (lint + biome + knip) runs in CI.
if [ -d ftc ] && [ -f ftc/package.json ]; then
  cd ftc
fi
bun run typecheck
