/**
 * Shared outer width for legal/policy pages (`/policies/*`, `/cookies`, `/refund`).
 * Same max width and horizontal padding as `PageContainer` (`page-container.tsx`).
 */
export const POLICY_PAGE_CONTAINER_CLASS =
  "container mx-auto max-w-7xl px-4 py-12 sm:px-6 sm:py-16 lg:px-8";
