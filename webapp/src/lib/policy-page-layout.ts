/**
 * Shared outer width for legal/policy pages (`/policies/*`, `/cookies`).
 * Wider than the previous `max-w-2xl` on large viewports; padding aligned with `PageContainer`.
 */
export const POLICY_PAGE_CONTAINER_CLASS =
  "container mx-auto max-w-3xl px-4 py-12 sm:px-6 sm:py-16 lg:px-8 lg:max-w-5xl xl:max-w-6xl";
